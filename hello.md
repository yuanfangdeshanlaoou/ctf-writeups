# hello — Crypto (100 pts)

赛道：Crypto · 解法：广义 Wiener 攻击（Generalized Wiener Attack）破解多项式环 RSA

## 题目

一个非标准 RSA 变体：公钥 (N, e)，其中 `R = Z_N[t]/(t^10 - 2)`，即系数模 N、未知数满足 `t^10 = 2` 的多项式环。私钥 d 远小于标准 RSA 的 d，题目只给了密文 c 和公钥参数。

## 攻击链路

标准 RSA 的 Wiener 攻击利用 `d < N^0.25` 时 `e/N` 的连分数能逼近 `k/d`。这道题把它推广到多项式环：

1. **环的阶近似**：在 `Z_N[t]/(t^10 - 2)` 里，欧拉函数不再是一个数而是一个多项式/数量，近似满足 `φ ≈ N^10`（10 个维度的乘积方向）。
2. **d 的规模**：题目暗示 `d ≈ N^2.5`，比标准 RSA 的 `d ≈ N` 大得多，但仍远小于 `N^10`，于是连分数攻击仍然成立。对 `e/N^10` 做连分数展开，扫描收敛子逼近 `k/d`。
3. **代数验真**：每取一个收敛子，猜测 φ，然后用"判别式是否开方 + p,q 是否整十次方根"验真。这一步是核心：不是所有收敛子都成立，必须用代数性质过滤假阳性。
4. **还原 p,q**：验真通过后得到 p、q（各 1023 位），从而得到环的真正指数阶。
5. **求环指数 L**：用 sympy 在 GF(p) 上分解 `x^10 - 2`，取各因子次数的最小公倍数（degs 5,5 / 1,1,4,4 → lcm），得到 `L = lcm(...)`。
6. **解密**：`x = e^{-1} mod L`，`m = c^x mod N` 还原明文，拼出 flag。

## 踩坑记录

- p,q 各 1023 位，不是 2043 位。从 N 的规模脑补错了方向，指数层错了两次。
- 手搓 DDF 多项式分解有代数 bug（次数和 > 10），必须用 `sympy Poly(GF(p)).factor_list()`，别自己造轮子。
- flag 尾码规则：`flag[11:-1]`，11 = `COMPFEST18{` 前缀长度；明文里是 `COMPFEST`（无 18），按平台前缀拼接才对。
- 验真时机：连分数收敛子扫描阶段就做代数验真（判别式开方 + 整十次方根），能提前砍掉 99% 的假候选。

## 实战映射

- **弱参数 RSA 的检测与破解**：真实世界里大量遗留系统用生成器产出的"看起来随机"的 RSA 参数，d 过小（私钥生成器 bug / CRT 优化不当）就是 Wiener 类攻击的现实猎物。能跑通广义 Wiener，面对任意非标准 RSA 实现都有系统化破解路径。
- **多项式环 / 代数数论**：区块链与后量子密码里大量用到多项式环（RLWE、格密码），懂 `Z_N[t]` 的分解思路，面对同态加密 / 零知识证明的数学底层不虚。
- **方法论**：连分数 + 代数验真 = "穷举候选 + 强性质过滤"的组合拳，这种模式在密码分析里反复出现。

## 产物

- `solve_hello_v6.py`（纯 Python 环算术栈：Kronecker 替换多项式乘法 + 对拍验证）
- 提交链路：`CTFd.fetch POST /api/v1/challenges/attempt {challenge_id, submission}` → `{"status":"correct"}`
